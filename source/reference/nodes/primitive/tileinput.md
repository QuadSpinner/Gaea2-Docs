---
description: Reads a set of tiles for 1:1 input into a tiled build, or combines it into a single input.
title: TileInput
uid: tileinput
order: 22
---

# TileInput

## When to Use It
Use TileInput when you need reads a set of tiles for 1:1 input into a tiled build, or combines it into a single input.

## Visual Direction
Lead with the raw output so the base pattern is clear. Follow with one terrain-context example where the result is blended, eroded, or reused as a mask.

## Properties
