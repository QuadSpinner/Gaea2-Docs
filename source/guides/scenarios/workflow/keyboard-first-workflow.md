---
title: Work Faster with Keyboard-First Navigation
uid: keyboard-first-workflow
order: 28
description: Learn the small set of Gaea keyboard habits that remove friction from graph work, export checks, and debugging.
---

# Work Faster with Keyboard-First Navigation

You do not need to memorize every shortcut in Gaea. A small set of keyboard habits removes most of the repeated mouse hunting: create nodes, rename outputs, suspend the engine, inspect Data View, group work, bypass tests, and manage portals.

## The shortcuts worth learning first

| Task | Shortcut |
| --- | --- |
| Lazy Menu | `F1` |
| Create/search nodes | `TAB` |
| Rename selected node | `F2` |
| Mark for export | `F3` |
| Chaning node shortcodes| `m,e1,ss,` |
| Suspend/resume engine | `` ` `` |
| Terrain tab in Data View | `Ctrl + F1` |
| Build tab in Data View | `Ctrl + F2` |
| Group selected nodes | `Ctrl + G` |
| Bypass selected node | `B` |
| Manage portals | `P` |
| Navigate to... connected nodes | `,` |

## Where they pay off

When a heavy graph keeps rebuilding, suspend the engine before editing several nodes. When output names are unclear, select the node and press `F2`. When you cannot find what will export, jump to the Build tab in Data View. When a branch may be damaging the result, bypass it with `B`. When wires run across the graph, use portals instead of dragging long connections through the whole terrain.

These are small habits, but they keep attention on the terrain instead of the interface.

## Confirm it in Gaea

This guide should include short capture examples:

* opening the Lazy Menu
* creating a node with search
* renaming an output node
* suspending the engine in a heavy graph
* using Data View to find export-marked nodes
* bypassing a suspect branch

The goal is not shortcut trivia. It is a faster daily rhythm for working through large graphs.
